ALLOWED_HOSTS=
DJANGO_DEBUG=
DJANGO_SECRET_KEY=
DJANGO_LOGLEVEL=
BOT_MODE=polling  # polling — запускает отдельный процесс бота; webhook — polling не запускается
DB_NAME=
DB_USER=
DB_PASSWORD=
DB_HOST=db  # в Docker — имя сервиса из docker-compose; локально — localhost или IP
DB_PORT=5432
CSRF_TRUSTED_ORIGINS=
LOGFILE_NAME=
LOGFILE_SIZE=
LOGFILE_COUNT=
# MAX_UPLOAD_SIZE_MB=5  # Размер в мегабайтах (например: 5, 10, 15)
